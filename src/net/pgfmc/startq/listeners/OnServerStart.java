package net.pgfmc.startq.listeners;

import net.pgfmc.startq.api.events.GenericServerEvent;
import net.pgfmc.startq.api.hooks.ServerListener;

public class OnServerStart implements ServerListener {

	@Override
	public void onEvent(GenericServerEvent event) {
		// XXX
	}

}
